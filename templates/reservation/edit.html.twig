{% extends 'base.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}
    <h1>Editer la réservation</h1>

    {{ include('reservation/_form.html.twig', {'button_label': 'Editer'}) }}

    <a href="{{ path('app_reservation_index') }}" class="btn btn-primary m-2">Retour à la liste</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
