{% extends 'base.html.twig' %}

{% block title %}Edit Voyage{% endblock %}

{% block body %}
    <h1>Editer Voyage</h1>

    {{ include('voyage/_form.html.twig', {'button_label': 'Editer'}) }}

    <a href="{{ path('app_voyage_index') }}" class="btn btn-primary m-2">Retour à la liste</a>

    {{ include('voyage/_delete_form.html.twig') }}
{% endblock %}
